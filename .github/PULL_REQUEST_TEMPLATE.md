## Summary

<!-- What and why (1–3 bullets). -->

## Test plan

- [ ] Manual check: …
- [ ] `cd apps/api && ruff check src` (if API touched)
- [ ] `cd apps/web && npm run lint` (if web touched)

## Notes

- Local-first: no required cloud APIs
- Do not commit `data/`, `.env`, or credentials
